# UI Interaction Report Checklist

Capture screenshots of Swagger UI, issue creation, issue listing, issue update, comment creation, GitHub webhook delivery, `/events`, `/healthz`, pytest coverage, Docker execution, and the passing CI workflow. Add captions explaining the input, expected result, and observed HTTP status.
